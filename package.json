{
  "name": "frigate-view-card",
  "version": "1.0.506",
  "private": true,
  "scripts": {
    "build": "node scripts/build.mjs",
    "check": "npm run build && node --check frigate-view-card.js",
    "test": "npm run build && node --test tests/*.test.mjs"
  },
  "devDependencies": {
    "esbuild": "^0.25.0"
  }
}
